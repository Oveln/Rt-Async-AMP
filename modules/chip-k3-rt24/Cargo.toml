[package]
name = "chip-k3-rt24"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
platform = { path = "../../rt-async/modules/platform" }
extern-trait = "0.4.3"
riscv = { version = "0.16.0", default-features = false, features = ["critical-section-single-hart"] }
fdt-parser = "0.4.19"
log = { version = "0.4", default-features = false }
ov-shm = { path = "../../modules/ov-shm" }
tock-registers = "0.10"

[build-dependencies]
xtask = { path = "../../xtask" }

[features]
riscv64 = ["platform/riscv64"]
# mailbox ISR 风暴诊断打印（默认关闭；开启会显著限速，仅联调定位用）
mbox-diag = []
