[package]
name = "user-test-mbox"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rtshm-abi = { path = "../rtshm-abi" }
libc = "0.2"

[build-dependencies]
xtask = { path = "../../xtask" }
