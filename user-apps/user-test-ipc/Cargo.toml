[package]
name = "user-test-ipc"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
ov-channels = { version = "0.1.0", features = ["std"] }
serde = { workspace = true }
postcard = { workspace = true }
libc = "0.2"

[build-dependencies]
xtask = { path = "../../xtask" }
